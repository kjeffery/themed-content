# Raw exports from the in-app debug tool — only the hashed copy written by
# tools/publish.py should be committed.
/closures.json
/graph.json
/weights.json
/menus.json
/hexgrid.json

# Local Python venv for tools/.
.venv/

# Python bytecode cache for tools/.
tools/__pycache__/
